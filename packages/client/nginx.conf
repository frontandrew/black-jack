events {
}

http {
    include mime.types;
    server {
        listen 3000;
        listen [::]:3000;

        location / {
            root /app;
            try_files $uri /index.html;
            add_header Access-Control-Allow-Origin *;
        }
    }
}